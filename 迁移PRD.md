# 霓虹乱斗前后端分离迁移 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 霓虹乱斗前后端分离版 |
| 当前版本基线 | `版本1`、`版本1_本地版`、`霓虹乱斗离线版` |
| 目标形态 | Vue 3 + TypeScript 前端，Python 后端 |
| 文档目的 | 明确迁移范围、目标结构、接口协议与验收标准 |
| 迁移原则 | 保留现有玩法和数值，优先拆分职责，再进行模块化重构 |

## 2. 背景与现状

当前三个目录均由 Python `aiohttp` 服务端和 `public` 原生 HTML/CSS/JavaScript 组成。服务端同时负责：

- 提供页面和静态资源；
- 提供 `/health` 健康检查；
- 通过 `/ws` 建立 WebSocket 长连接；
- 管理房间、玩家、移动、射击、伤害、击杀、复活、道具、地形和聊天；
- 离线版额外管理人机、难度和寻路。

三个版本的核心战斗逻辑基本一致，主要差异如下：

| 版本 | 主要差异 |
| --- | --- |
| 互联网版 | 公网访问、FRP/Cloudflare 启动脚本，协议版本为当前互联网版协议 |
| 本地版 | 局域网访问，不启动公网隧道 |
| 离线版 | 仅监听本机，增加 0-11 个人机和简单/普通/困难难度 |

## 3. 产品目标

### 3.1 必须实现

1. 前端独立构建、独立开发和独立部署，不依赖 Python 后端返回 HTML。
2. 前端使用 Vue 3、TypeScript 和 Vite，代码按页面、组件、状态、网络和渲染职责拆分。
3. 后端使用 Python，提供 REST 健康检查/配置接口和 WebSocket 实时游戏接口。
4. 服务端继续作为游戏规则和战斗状态的唯一权威来源。
5. 互联网版、本地版、离线版统一为同一套代码，通过配置切换能力和运行方式。
6. 保留现有三版已经支持的核心玩法、移动端双摇杆、聊天、职业、道具和战斗表现。

### 3.2 不在本次迁移范围

- 不重新设计游戏规则、地图布局、职业数值和道具数值；
- 不引入账号、登录、数据库、排行榜或持久化存档；
- 不把客户端预测变成客户端权威判定；
- 不要求本次迁移同时完成美术重制；
- 不在代码仓库中提交 FRP 密钥、Cloudflare 凭据或其他敏感配置。

## 4. 迁移内容

### 4.1 前端迁移

- 将 `public/index.html` 拆分为 Vue 应用入口、菜单页、游戏场景、HUD、聊天面板、职业选择、人机设置和移动端控制组件。
- 将现有 `game.js` 拆分为 TypeScript 模块：WebSocket 客户端、输入采集、状态管理、客户端预测、画布渲染、移动端适配和 UI 事件。
- 将现有 `style.css` 拆分为全局样式、菜单样式、战斗 HUD 样式、聊天样式和触控布局样式。
- 使用环境变量配置后端地址，支持 HTTP/HTTPS 与 WS/WSS 自动匹配。
- 保留键盘、鼠标、触摸双摇杆、横屏提示、安全区适配、延迟显示和断线提示。
- 所有来自聊天或服务端的文本必须通过 Vue 模板文本绑定渲染，禁止拼接未经处理的 HTML。

### 4.2 后端迁移

- 从单文件 `server.py` 拆出配置、HTTP 路由、WebSocket 会话、房间管理、游戏实体、战斗系统、道具系统、职业系统、聊天校验和离线人机模块。
- 保留 Python 实现和服务端权威模拟，物理循环目标为 30 tick/s，网络状态广播频率按运行配置保持互联网版 25 帧/s、本地/离线版 30 帧/s 的现有行为。
- 后端只提供 JSON/JSONL WebSocket 消息，不再负责前端页面渲染和静态文件托管。
- `/health` 返回运行模式、服务状态和协议版本，便于启动脚本、容器和监控检查。
- 互联网、本地、离线能力通过 `APP_EDITION` 或等效配置选择；离线人机模块只能在 offline 模式启用。
- 保留现有输入序号、输入超时刹车、服务端碰撞检测、重连/断开清理和房间人数上限逻辑。

### 4.3 运行与部署迁移

- 前端开发服务器和 Python API/WebSocket 服务分别启动。
- 生产环境前端构建为静态资源，可部署到 Nginx、CDN 或静态托管服务；后端单独部署并支持 WebSocket 反向代理。
- 本地版启动脚本负责启动后端并输出前端访问地址；互联网版启动脚本负责后端和 FRP/Cloudflare 隧道；离线版只允许本机访问并保留人机配置。
- Docker 镜像至少提供后端镜像；前端可独立构建为静态镜像，也可由部署平台托管。
- 前后端必须分别提供 `.env.example`，实际密钥和本机路径不得提交。

## 5. 功能需求

### 5.1 房间与连接

- 玩家输入昵称、房间号和玩法模式后加入房间。
- 同一房间必须使用相同玩法模式；房间最多 12 名真人/人机占用名额。
- 加入成功返回玩家 ID、房间号、运行版本、协议版本、地图尺寸、初始地形和网络频率。
- 连接断开后移除玩家、清理其子弹/激光/人机占用，并重新计算道具数量。
- 连接异常、房间满、模式冲突、非法输入必须返回可展示的错误消息，不得导致服务进程退出。

### 5.2 战斗与玩法

保留现有以下行为：

- 经典、多道具、纯净、职业四种模式；
- WASD/方向键移动、鼠标瞄准、按住左键射击；
- 手机左摇杆移动、右摇杆瞄准并持续射击；
- 普通武器、激光、反射激光、高能光束、三重射击、反弹弹药和攻城炮；
- 伤害、护盾、无敌、击杀计分、2.5 秒复活、地形摧毁与恢复；
- 坦克、法师、狙击手、死灵法师、武器大师、圣骑士；
- 房间聊天、消息长度限制、发送冷却和输入框聚焦时停止游戏输入；
- 离线版人机数量、难度、寻路、拾取道具、战斗、计分和职业选择。

### 5.3 协议要求

协议应集中定义在 `shared/protocol` 或后端协议模块中，禁止在多个组件中散落字符串和字段名。至少包含：

| 方向 | 消息类型 | 关键字段 |
| --- | --- | --- |
| 前端 -> 后端 | `join` | `name`、`room`、`mode` |
| 前端 -> 后端 | `input` | `seq`、移动方向/向量、`angle`、`shoot`、`ability` |
| 前端 -> 后端 | `role` | `role` |
| 前端 -> 后端 | `chat` | `message` |
| 前端 -> 后端 | `bots` | `count`、`difficulty`，仅 offline |
| 前端 -> 后端 | `ping` | `sent` |
| 后端 -> 前端 | `welcome` | `player_id`、`room`、`mode`、`protocol`、`obstacles` |
| 后端 -> 前端 | `state` | 玩家、子弹、激光、爆炸、道具、摧毁地形、服务器时间/输入确认 |
| 后端 -> 前端 | `chat` | `name`、`message`、`color` |
| 后端 -> 前端 | `pong` | 原样返回 `sent` |
| 后端 -> 前端 | `error` | `code`、用户可读 `message` |

状态帧应继续使用增量地形同步，初始地形只在 `welcome` 发送，后续仅发送被摧毁或恢复的地形块。所有外部输入必须做类型、范围、长度和频率校验。

## 6. 目标目录结构

建议在仓库根目录新增统一项目 `neon-brawl`，迁移完成后三个旧目录仅作为迁移参考或归档，不再继续分别维护业务代码。

```text
neon-brawl/
├─ frontend/
│  ├─ public/
│  ├─ src/
│  │  ├─ app/
│  │  ├─ components/
│  │  │  ├─ BattleCanvas/
│  │  │  ├─ ChatPanel/
│  │  │  ├─ Hud/
│  │  │  ├─ JoinMenu/
│  │  │  ├─ MobileControls/
│  │  │  ├─ RoleSelector/
│  │  │  └─ BotSettings/
│  │  ├─ composables/
│  │  ├─ network/
│  │  ├─ render/
│  │  ├─ stores/
│  │  ├─ types/
│  │  ├─ styles/
│  │  ├─ App.vue
│  │  └─ main.ts
│  ├─ index.html
│  ├─ package.json
│  ├─ tsconfig.json
│  ├─ vite.config.ts
│  └─ .env.example
├─ backend/
│  ├─ app/
│  │  ├─ api/
│  │  │  ├─ health.py
│  │  │  └─ websocket.py
│  │  ├─ config.py
│  │  ├─ protocol/
│  │  ├─ rooms/
│  │  ├─ game/
│  │  │  ├─ entities.py
│  │  │  ├─ physics.py
│  │  │  ├─ combat.py
│  │  │  ├─ pickups.py
│  │  │  ├─ roles.py
│  │  │  └─ terrain.py
│  │  ├─ bots/
│  │  └─ main.py
│  ├─ tests/
│  │  ├─ unit/
│  │  ├─ integration/
│  │  └─ fixtures/
│  ├─ requirements.txt
│  ├─ Dockerfile
│  └─ .env.example
├─ shared/
│  └─ protocol/
│     └─ protocol.md
├─ scripts/
│  ├─ start_local.ps1
│  ├─ start_internet.ps1
│  ├─ start_offline.ps1
│  └─ smoke_test.ps1
├─ docs/
│  ├─ deployment.md
│  └─ protocol.md
├─ docker-compose.yml
└─ README.md
```

### 6.1 目录职责约束

- `frontend/components` 只负责界面和用户交互，不直接修改服务端游戏状态。
- `frontend/stores` 保存可展示的客户端状态；网络消息由 `frontend/network` 统一解析后写入。
- `frontend/render` 只负责 Canvas 绘制和本地预测展示，不承担伤害、击杀或碰撞判定。
- `backend/game` 是玩法规则唯一实现位置；不同版本不得复制一套 `Room` 逻辑。
- `backend/api` 只负责连接生命周期、消息路由和输入校验。
- `backend/bots` 只在离线模式装配，不污染真人联机路径。
- `shared/protocol` 记录消息结构、字段类型、范围、版本兼容策略和示例。

## 7. 迁移策略与阶段产物

### 阶段一：协议与基线冻结

- 整理三个版本当前协议字段、健康检查、玩法数值和已有 `smoke_test.py`。
- 确定统一协议版本和 internet/local/offline 运行配置。
- 产物：协议文档、数值清单、现有测试可重复运行。

### 阶段二：后端拆分

- 从现有 `server.py` 抽取游戏核心、房间、协议和 WebSocket 连接层。
- 先保证不改变行为，再加入类型标注、输入校验和单元测试。
- 产物：独立启动的 Python 后端，REST 健康检查和 WebSocket 兼容。

### 阶段三：Vue 前端替换

- 先接入加入房间、welcome/state、输入和退出流程，再迁移聊天、职业、移动端和离线人机 UI。
- 用 TypeScript 类型约束协议消息，保留 Canvas 渲染与预测逻辑。
- 产物：Vite 开发环境和可部署的静态前端。

### 阶段四：三版本统一与部署

- 删除前端对后端静态文件路径的依赖。
- 将启动脚本、Docker、反向代理和公网隧道改为读取统一配置。
- 完成旧目录对照测试后，将旧版本标记为归档。

## 8. 验收标准

### 8.1 结构与工程验收

- [ ] 前端可以在 `frontend` 目录独立执行安装、开发和生产构建，构建产物不包含 Python 源码。
- [ ] 后端可以在 `backend` 目录独立启动，不依赖前端源码或旧版本目录。
- [ ] 三个版本不再存在重复的核心房间/战斗实现，版本差异由配置或能力开关控制。
- [ ] 协议字段有统一定义，前端 TypeScript 和后端校验与文档一致。
- [ ] 生产配置、FRP/Cloudflare 凭据和本机路径均不进入版本库。
- [ ] 项目 README 能说明安装、开发、测试、构建、本地/互联网/离线启动方式。

### 8.2 后端功能验收

- [ ] `GET /health` 返回 `status=ok`、运行版本和协议版本；服务异常时进程不会因单个非法消息退出。
- [ ] 两个 WebSocket 客户端使用同一房间号可以收到同一状态；不同模式加入同一房间会被拒绝。
- [ ] 房间最多容纳 12 个名额；玩家离开后状态、道具和连接资源正确清理。
- [ ] 服务端正确处理移动、碰撞、射击、伤害、击杀、复活、职业、道具、激光、爆炸和地形恢复。
- [ ] 过期输入序号被忽略；输入中断超过现有约定时间后自动停止移动和射击。
- [ ] 非法昵称、房间号、模式、坐标、角度、消息长度和人机数量均被安全限制。
- [ ] 聊天消息按现有规则清理空白、限制长度和发送频率，客户端不会执行消息中的 HTML。
- [ ] 离线版支持 0-11 个人机和三档难度；人机可以移动、绕障碍、拾取、射击、计分和复活。

### 8.3 前端功能验收

- [ ] Vue 3 页面可完成加入房间、战斗、职业选择、聊天、退出和错误提示完整流程。
- [ ] 键盘/鼠标和手机双摇杆均可操作；聊天输入聚焦时不会误触发移动或射击。
- [ ] Canvas 正确渲染玩家、生命值、分数、子弹、激光、爆炸、道具、地形和摧毁/恢复状态。
- [ ] 客户端预测和服务端校正存在，断线、延迟、后台切换和输入松开不会造成持续移动。
- [ ] 手机横屏提示、安全区、不同视口尺寸和高分屏渲染均可正常工作，无主要控件遮挡。
- [ ] 前端通过 `VITE_API_BASE_URL` 或等效变量连接不同后端环境，HTTP/HTTPS 能正确对应 WS/WSS。

### 8.4 回归与性能验收

- [ ] 迁移后的单元测试覆盖碰撞、激光反射、伤害减免、道具、职业、人机和地形恢复等原有 smoke test 场景。
- [ ] 集成测试覆盖两客户端联机、四种玩法模式、聊天、延迟 ping、断开清理和离线人机配置。
- [ ] 在 12 名额满房情况下，服务端物理循环稳定运行，状态广播无未处理异常；客户端无持续增长的 WebSocket/Canvas 资源泄漏。
- [ ] 本地版可在局域网访问，互联网版可通过现有隧道访问，离线版只监听 `127.0.0.1`；三种模式的访问边界符合预期。
- [ ] 新旧版本在相同测试场景下的关键数值一致：移动速度、伤害、复活时间、道具数量、职业属性和地形恢复时间。

## 9. 交付物

- `frontend/` Vue 3 + TypeScript + Vite 前端源码与构建配置；
- `backend/` Python 后端源码、依赖、测试和 Docker 配置；
- `shared/protocol/` 协议定义和版本说明；
- 三种运行模式的启动脚本与环境变量示例；
- 单元测试、WebSocket 集成测试和 smoke test；
- 开发、部署、故障排查和协议文档；
- 迁移对照表，记录旧文件到新模块的对应关系以及已知兼容差异。

## 10. 风险与处理

| 风险 | 处理方式 |
| --- | --- |
| 三个版本长期分叉，迁移时行为不一致 | 先冻结协议和数值，以互联网版为主基线，离线人机作为可插拔模块迁移 |
| WebSocket 字段变更导致前端无法连接 | 协议集中定义、增加协议版本、保留集成测试和兼容错误提示 |
| 前端预测与服务端状态抖动 | 迁移阶段只移动代码边界，不同时重写预测算法；逐项保留现有输入序号和校正逻辑 |
| 前后端分离后跨域或 WSS 配置错误 | 提供开发代理和生产反向代理示例，健康检查与浏览器连接测试纳入验收 |
| 离线人机逻辑影响真人联机 | 人机服务只在 offline 配置装配，后端启动时明确记录运行版本 |
| 公网部署暴露服务或密钥 | 默认最小监听范围、环境变量注入凭据、启动脚本禁止打印 token |
